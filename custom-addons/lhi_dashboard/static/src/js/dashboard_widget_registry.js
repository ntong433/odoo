/** @odoo-module **/

import { Registry } from "@web/core/registry";

/**
 * Registry for LHI Dashboard Widgets.
 * Other modules can add their own widgets here without modifying the core dashboard.
 */
export const dashboardWidgetRegistry = new Registry();
